{
	auto_https disable_redirects
}

{$BUDGET_HOST} {
	tls internal
	encode gzip

	@api path /api /api/*
	handle @api {
		reverse_proxy backend:8000
	}

	handle {
		root * /srv
		try_files {path} /index.html
		file_server
	}
}
